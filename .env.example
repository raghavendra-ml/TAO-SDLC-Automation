### Example env values for local development and ngrok
# For local development (dev server with proxy to backend at http://localhost:8000)
VITE_API_BASE_URL=http://localhost:8000

# If you use ngrok and want the frontend to call the ngrok URL directly:
# VITE_API_BASE_URL=https://abcd-1234-xyz.ngrok-free.dev

# If you prefer to keep '/api' in the frontend base path, leave VITE_API_BASE_URL empty
# and use the dev server proxy (default behavior):
# VITE_API_BASE_URL=
